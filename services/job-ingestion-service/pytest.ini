[pytest]
testpaths = tests
addopts =
    --strict-config
    --strict-markers
    --cov=job_ingestion
    --cov-branch
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=80
markers =
    integration: requires a PostgreSQL test database
