[pytest]
testpaths = tests
addopts =
    --strict-config
    --strict-markers
    --cov=platform_skills
    --cov-branch
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=80
